<h1>Author:<%= @review.author %></h1>
<h5>Review:<%= @review.content_body %></h5>
<h3>Rating:<%= @review.content_body %></h3>


<p><%= link_to "Edit", edit_product_review_path(@review, @product) %></p>
<p><%= link_to "Delete review", product_review_path(@review, @product),
                        :data => {:confirm => "Are you sure?",
                                       :method => "delete"} %></p>

<p><%= link_to "Return to product", product_path(@product) %></p>